<div>
    <h2 style="color:crimson;">Productos en venta</h2>

    {{#if productsDTO}}
    <div class="table-responsive">
        <table class="table table-dark" id="on-sale-products">
    
        </table>
    </div>
    {{else}}
        <h3 class="alert alert-warning"> No se encontraron productos para vender</h3>
    {{/if}}
</div>